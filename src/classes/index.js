// Sirve para agrupar todas las clases que están capeta "clases" y exportarlas
// Lugar centralizado de clases.

import {Todo} from './todo.class';
import {TodoList} from './todo.list.class';

export{Todo, TodoList};